# randInt() returns a random integer between 0 and 100
import random

def randInt100():
    randomizedNum = random.random()*100
    return int(randomizedNum)

# randInt(max=50) returns a random integer between 0 to 50

def randInt50()
    randomizedNum = random.random()*50
    return int(randomizedNum)

# randInt(min=50) returns a random integer between 50 and 100

def randInt50to100():
    randomizedNum = 50 + (random.random()*100)
    return int(randomizedNum)

# randInt(min=50, max=500) returns a random integer between 50 and 500

def randInt50to500():
    randomizedNum = 50 + (random.random()*500)
    return int(randomizedNum) 